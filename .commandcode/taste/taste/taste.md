# Taste
- User communicates in Bahasa Indonesia (e.g., "coba review overall websitenya", "coba perbaiki semua sekarang") — respond in Indonesian, including artifacts like review reports. Confidence: 0.8
- After the agent produces a diagnostic (e.g., a design review report), the user wants ALL findings fixed immediately in one autonomous pass ("coba perbaiki semua sekarang") — no per-item confirmation, no waiting for approval; proceed comprehensively and report what was intentionally left out. Confidence: 0.7
- Explicit workflow directive (stated directly): minimize ceremony and work fast — don't over-update todo lists, batch edits into as few calls as possible, skip verification/screenshot steps that aren't essential, and execute directly without long explanations up front. Confidence: 0.95
- Git workflow: push straight to `main` on origin (user: "tolong push ke main") — no branch/PR ceremony; single commit + direct push is the expected delivery. Confidence: 0.6
- Open to installing global dev tools (e.g., agent-browser via npm/Homebrew) when the agent asks permission first — approved the install when asked. Always ask before global/system installs. Confidence: 0.6
- Environment: macOS with no Node.js/npm on shell PATH; Homebrew exists at /opt/homebrew/bin but is not on the shell PATH (invoke tools by full path). Confidence: 0.8
- Brave Browser is installed and can be driven headless via `--remote-debugging-port` + CDP as a fallback when browser-automation browsers can't be downloaded (Chromium download failed due to network timeouts). Confidence: 0.7
- User pushes for top-tier visual quality with explicit numeric targets (set a "10/10" hierarchy goal via /skill:design) — treat design scores as a bar to actually reach, not a soft suggestion; be honest about what remains out of scope. Confidence: 0.7
- Typography floor: no labels below 10px (all 7/8/9px micro-labels were raised to 10px across markup and JS). Keep a stepped type scale where each size has one clear role (here: 10/11/13/16/20 — uppercase micro-label vs. font-black value vs. title). Confidence: 0.7
- Hierarchy rule: the primary CTA must be the single loudest element in every view (e.g., "Kirim" went from muted bg-white/10 to solid emerald, font-black, uppercase, shadow), and CTA colors should encode purpose consistently with the existing palette (emerald = product/feed actions, amber = game actions). Validate with a squint test (what reads 1st/2nd/3rd per view) plus real-browser screenshots at 390px and 320px, including DOM overflow checks after size increases. Confidence: 0.7
